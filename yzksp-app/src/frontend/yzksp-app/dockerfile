# ベースイメージとしてNode.jsの公式イメージを使用
FROM node:14-alpine

# 作業ディレクトリを設定
WORKDIR /app

# パッケージファイルをコピー
COPY package.json package-lock.json ./

# 依存関係をインストール
RUN npm install
RUN npm install axios

# アプリケーションのソースコードをコピー
COPY . .

# アプリケーションをビルド
RUN npm run build

# 静的ファイルを提供するためにserveをインストール
RUN npm install -g serve

# アプリケーションを実行
CMD ["serve", "-s", "build"]